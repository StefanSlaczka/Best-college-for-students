
  //hooks
//Get data form makming a hook for this page
//make a hook to gain the llocation only for state to not overload the page

const moreThanOneChoice = () => {
  //collects data from hook and then make table row with the data
}

const pageanaisan = () => {
  //makes pages and orgnise it
}

const getLocastion = () => {
  //get locaiton form hook and auto fill it in the state dropdown only one time
}


function App() {
  return (
    <div name="top">
      <h2 name="logo">logo</h2>
      <div nam="State dropdown">state</div>
      <button name="getLocation">find My state</button>
      <div name="college-name search">search</div>
      <div name="table">
        <table>
          <tr name="copy">
            <th name="cost">criedit pay</th>
            <th name="jobs">employent %</th>
            <th name="total">total criedits  </th>
            <th name="tuff">difcluty</th>
            <th name="path">path the college choices</th>
            <th name="time">total time</th>
          </tr>
          {/*make a for lop for this  if there is any more colleges*/}
        </table>
      </div>
    </div>
  );
}

export default App;
